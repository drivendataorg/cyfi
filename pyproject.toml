[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "cyano"
version = "0.1.0"
authors = [
  {name = "DrivenData", email = "info@drivendata.org"}
]
description = "Cyano is a Python package to predict cyanobacteria density at specific locations based on satellite imagery and other publicly available data sources."
readme = "README.md"
license = {text = "MIT License"}
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
requires-python = ">=3.10"

[tool.black]
line-length = 99
target-version = ['py310', 'py311']
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.venv
)/
'''

[project.scripts]
cyano = "cyano.cli:app"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--cov=cyano --cov-report=term --cov-report=html --cov-report=xml"
testpaths = ["tests"]

[tool.coverage.run]
source = ["cyano"]
